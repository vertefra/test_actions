name: Build and Push

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
  
    steps:
      - name: Build and Push
        id: test_actions
        uses: docker/build-push-action@v2
        with:
          push: true
          tags: vertefra/test_actions:latest